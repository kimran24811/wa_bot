# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/cdkbot

# JWT secret — use a long random string
JWT_SECRET=change_this_to_a_random_secret

# Server port
PORT=3000

# Node environment
NODE_ENV=production
